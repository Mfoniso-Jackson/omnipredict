{
  "name": "omnipredict",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TxLINE World Cup prediction market intelligence dashboard.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs --host 127.0.0.1 --port 4173",
    "test": "node tests/analytics.test.mjs",
    "check": "node --check app.js && node --check src/analytics/market-engine.js && node --check src/ai/explanation-engine.js && node --check src/adapters/txline-adapter.js && node --check src/portfolio/portfolio-engine.js && node --check src/settlement/settlement-engine.js && node --check scripts/dev-server.mjs"
  },
  "keywords": [
    "prediction-markets",
    "txline",
    "world-cup",
    "solana",
    "market-intelligence"
  ],
  "engines": {
    "node": ">=18"
  }
}
